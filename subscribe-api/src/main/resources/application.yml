server:
  port: ${PORT:8000}
spring:
  application:
    name: subscribe-api
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
